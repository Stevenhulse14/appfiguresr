import HomePage from "@/components/HomePage";

/**
 * Root page component that serves as the entry point
 * Delegates rendering to the HomePage component
 */
export default function Page() {
  return <HomePage />;
}
